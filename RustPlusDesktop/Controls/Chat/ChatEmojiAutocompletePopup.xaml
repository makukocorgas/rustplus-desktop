<UserControl x:Class="RustPlusDesk.Controls.Chat.ChatEmojiAutocompletePopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Border Background="#1A1F29" BorderBrush="#2D384D" BorderThickness="1" CornerRadius="8" Padding="6"
            MaxWidth="360" MinWidth="240">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="4" Color="Black" Opacity="0.65"/>
        </Border.Effect>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="220" Focusable="False">
            <ItemsControl x:Name="ItemsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="ItemRow" Margin="1,2" Padding="6,4" CornerRadius="6" Background="#131722" BorderBrush="#252F42" BorderThickness="1"
                                Cursor="Hand"
                                MouseLeftButtonDown="ItemRow_MouseLeftButtonDown"
                                MouseEnter="ItemRow_MouseEnter">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="34"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="{Binding Icon}" Width="30" Height="30"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,4,0">
                                    <TextBlock Text="{Binding Tag}" Foreground="#3FD7FF" FontSize="12"
                                               FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding DisplayName}" Foreground="#7E8B9F" FontSize="10"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Border>
</UserControl>
