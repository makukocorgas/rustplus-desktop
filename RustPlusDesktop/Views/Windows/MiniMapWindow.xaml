<Window x:Class="RustPlusDesk.MiniMapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:p="clr-namespace:RustPlusDesk.Properties"
        xmlns:views="clr-namespace:RustPlusDesk.Views"
        xmlns:tutorials="clr-namespace:RustPlusDesk.Features.Tutorials"
        xmlns:tutorialControls="clr-namespace:RustPlusDesk.Features.Tutorials.Controls"
        Title="{DynamicResource MiniMap}"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="260" Height="260">
    <Grid x:Name="RootGrid">

        <!--  Drawn only while a tile is being dragged, and behind everything: the cells it can
              land in, plus the one it would land in right now. Without it a tile let go in an
              empty area snapped somewhere with no visible reason.  -->
        <Canvas x:Name="GridGhostLayer" IsHitTestVisible="False"/>

        <!--  An outline of a saved arrangement, faded in while its name is hovered in the
              templates window.  -->
        <Canvas x:Name="PresetPreviewLayer" IsHitTestVisible="False"/>

        <!--  The dock: the map is one tile among the command tiles, all placed by pixel
              position on this canvas. Tiles are built in code because their number, kind
              and position come from the saved layout.  -->
        <Canvas x:Name="DockCanvas" Background="Transparent">

            <!-- Container that has the exact size of the Map shape -->
            <Grid x:Name="MapContainer" Width="260" Height="260">
                <!-- Runde / Eckige Mini-Map -->
                <Border x:Name="MapShapeBorder" CornerRadius="130" BorderBrush="#66000000" BorderThickness="1" SnapsToDevicePixels="True">
                    <!--  One mirror per map layer instead of a single brush over the whole map
                          host. A VisualBrush shows a visual subtree whole or not at all, so the
                          only way to switch texture, grid, drawings, icons and players
                          independently is to give each its own brush. All five share the same
                          absolute viewbox because the source canvases are siblings in the map
                          scene and therefore share its coordinate space.  -->
                    <Grid x:Name="MapClipHost">
                        <Rectangle x:Name="MapBackdrop" Fill="#0B3A4A"/>
                        <Rectangle x:Name="LayerTexture">
                            <Rectangle.Fill>
                                <VisualBrush x:Name="BrushTexture" Stretch="Uniform" AlignmentX="Center" AlignmentY="Center"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <!--  Over the texture, as on the main map, but its own layer: with the
                              texture switched off the heatmap still has to show, which is the
                              case it is most useful in.  -->
                        <Rectangle x:Name="LayerHeatmap">
                            <Rectangle.Fill>
                                <VisualBrush x:Name="BrushHeatmap" Stretch="Uniform" AlignmentX="Center" AlignmentY="Center"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <Rectangle x:Name="LayerGrid">
                            <Rectangle.Fill>
                                <VisualBrush x:Name="BrushGrid" Stretch="Uniform" AlignmentX="Center" AlignmentY="Center"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <Rectangle x:Name="LayerDrawings">
                            <Rectangle.Fill>
                                <VisualBrush x:Name="BrushDrawings" Stretch="Uniform" AlignmentX="Center" AlignmentY="Center"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <Rectangle x:Name="LayerIcons">
                            <Rectangle.Fill>
                                <VisualBrush x:Name="BrushIcons" Stretch="Uniform" AlignmentX="Center" AlignmentY="Center"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <!--  Above the icons but below the players: a death marker is a place,
                              and a teammate standing on it should still be visible.  -->
                        <Rectangle x:Name="LayerDeaths">
                            <Rectangle.Fill>
                                <VisualBrush x:Name="BrushDeaths" Stretch="Uniform" AlignmentX="Center" AlignmentY="Center"/>
                            </Rectangle.Fill>
                        </Rectangle>
                        <Rectangle x:Name="LayerPlayers">
                            <Rectangle.Fill>
                                <VisualBrush x:Name="BrushPlayers" Stretch="Uniform" AlignmentX="Center" AlignmentY="Center"/>
                            </Rectangle.Fill>
                        </Rectangle>
                    </Grid>
                </Border>

                <!-- Overlays Container -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,12">

                    <!-- Time Overlay -->
                    <Border x:Name="TimeOverlayBorder"
                            Background="#AA000000"
                            CornerRadius="6"
                            Padding="6,4"
                            Margin="0,0,4,0"
                            Visibility="Collapsed"
                            IsHitTestVisible="False">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{Binding DayNightColor}" Text="{Binding DayNightIcon}" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtServerTime" Text="{Binding ServerTime}" FontSize="12" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Pop Overlay -->
                    <Border x:Name="PopOverlayBorder"
                            Background="#AA000000"
                            CornerRadius="6"
                            Padding="6,4"
                            Visibility="Collapsed"
                            IsHitTestVisible="False">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{DynamicResource TextSubtle}" Text="&#xE716;" Margin="0,0,4,0" VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtServerPop" Text="{Binding ServerPlayers}" FontSize="12" Foreground="White" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

            </Grid>
        </Canvas>

        <!--  The dock's title bar: the handle for the whole grid, and the home of the add and
              settings buttons. It spans the dock and overlays whatever is under it, map tile
              included — so it exists and can be grabbed even with the map switched off, which
              a handle sitting inside the map tile could not.

              Its opacity is driven from code rather than an IsMouseOver trigger, because it
              appears on the same delay that arms the tile handles: a bar that showed up the
              instant the cursor crossed it would bring the delete buttons with it.  -->
        <Canvas x:Name="ChromeLayer">
            <Border x:Name="DockTitleBar"
                    Height="30"
                    Opacity="0"
                    IsHitTestVisible="False"
                    CornerRadius="8,8,0,0"
                    Background="#B0101518"
                    BorderBrush="#33FFFFFF"
                    BorderThickness="1,1,1,0"
                    Cursor="SizeAll">
                <Grid>
                    <!--  Grip dots, the usual sign for "this is draggable".  -->
                    <TextBlock x:Name="DockGrip"
                               Text="&#xE700;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="13"
                               Foreground="#99FFFFFF"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center"
                               Margin="10,0,0,0"
                               IsHitTestVisible="False"/>

                    <TextBlock x:Name="DockTitleText"
                               Text="{DynamicResource CommandDockTitle}"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Foreground="#99FFFFFF"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center"
                               Margin="30,0,0,0"
                               TextTrimming="CharacterEllipsis"
                               IsHitTestVisible="False"/>

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Margin="0,0,4,0">
                        <!--  The lock is what decides whether hovering a tile offers to move,
                              resize or delete it. Locked is the resting state.  -->
                        <ui:Button x:Name="BtnLockDock"
                                   Padding="3"
                                   Margin="0,0,2,0"
                                   Appearance="Transparent"
                                   Foreground="White"
                                   Click="BtnLockDock_Click">
                            <TextBlock x:Name="LockGlyph"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="14"
                                       Text="&#xE72E;"/>
                        </ui:Button>
                        <ui:Button x:Name="BtnTemplates"
                                   Padding="3"
                                   Margin="0,0,2,0"
                                   Appearance="Transparent"
                                   Foreground="White"
                                   Click="BtnTemplates_Click"
                                   ToolTip="{DynamicResource CommandDockTemplates}"
                                   Icon="{ui:SymbolIcon Symbol=Save24}"/>
                        <ui:Button x:Name="BtnAddTile"
                                   Padding="3"
                                   Margin="0,0,2,0"
                                   Appearance="Transparent"
                                   Foreground="White"
                                   Click="BtnAddTile_Click"
                                   ToolTip="{DynamicResource CommandDockAddTile}"
                                   Icon="{ui:SymbolIcon Symbol=Add24}"/>
                        <ui:Button x:Name="BtnSettings"
                                   Padding="3"
                                   Appearance="Transparent"
                                   Foreground="White"
                                   Click="BtnSettings_Click"
                                   tutorials:Tutorial.TargetId="MiniMap.Settings"
                                   Icon="{ui:SymbolIcon Symbol=Settings24}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Canvas>

        <!--  Settings live in a popup, not in this window. The size slider resizes the window
              it would otherwise sit in, which dragged the panel out from under the cursor on
              every tick. A popup has its own HWND: it is never clipped by the window and its
              screen position is ours to hold still while the map behind it grows.  -->
        <Popup x:Name="SettingsPopup"
               Placement="RelativePoint"
               PlacementTarget="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
               StaysOpen="True"
               AllowsTransparency="True"
               Focusable="False">
            <views:MiniMapSettingsOverlay x:Name="SettingsOverlay"/>
        </Popup>

        <!--  One panel, retargeted at whichever tile's gear was pressed. Its contents are built
              in code because the kind-specific half differs per tile.  -->
        <Popup x:Name="TileSettingsPopup"
               Placement="RelativePoint"
               PlacementTarget="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
               StaysOpen="True"
               AllowsTransparency="True"
               Focusable="False">
            <Border Background="{DynamicResource Surface}"
                    BorderBrush="{DynamicResource CardBorder}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="14">
                <Grid>
                    <ContentControl x:Name="TileSettingsPanel"/>
                    <ui:Button HorizontalAlignment="Right"
                               VerticalAlignment="Top"
                               Margin="0,-4,-4,0"
                               Padding="4"
                               Appearance="Transparent"
                               Click="TileSettingsClose_Click"
                               Icon="{ui:SymbolIcon Symbol=Dismiss24}"/>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</Window>
