<Window x:Class="RustPlusDesk.Views.Windows.BaseNoteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Edit Base Note"
        Width="400" Height="280"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True" 
        WindowStyle="None"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Border Background="{DynamicResource SurfaceAlt}" 
            BorderBrush="{DynamicResource CardBorder}" 
            BorderThickness="2" 
            CornerRadius="12">
        <DockPanel Margin="20">
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                <DockPanel>
                    <TextBlock x:Name="TxtTitle"
                               Text="Base Note" 
                               FontSize="18" FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimary}"
                               VerticalAlignment="Center"/>
                    
                    <Button Content="✕" 
                            HorizontalAlignment="Right" 
                            Click="BtnCancel_Click"
                            Background="Transparent"
                            BorderThickness="0"
                            Foreground="{DynamicResource TextSubtle}"
                            FontSize="16"
                            Cursor="Hand"
                            VerticalAlignment="Center"/>
                </DockPanel>
                <Separator Background="{DynamicResource CardBorder}" Margin="0,8,0,0"/>
            </StackPanel>
 
            <!-- Bottom Buttons -->
            <Grid DockPanel.Dock="Bottom" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Button x:Name="BtnSave" Grid.Column="1" Content="Save" Appearance="Primary" Width="100" Height="32" Margin="0,0,8,0" Click="BtnSave_Click" IsDefault="True"/>
                <ui:Button x:Name="BtnCancel" Grid.Column="2" Content="Cancel" Appearance="Secondary" Width="100" Height="32" Click="BtnCancel_Click" IsCancel="True"/>
            </Grid>
 
            <!-- Main Content -->
            <Grid>
                <TextBox x:Name="TxtNote" 
                         Background="{DynamicResource Surface}" 
                         Foreground="{DynamicResource TextPrimary}"
                         BorderBrush="{DynamicResource CardBorder}"
                         BorderThickness="1"
                         Padding="8,6"
                         AcceptsReturn="False" 
                         TextWrapping="NoWrap" 
                         MaxLength="100"
                         FontSize="13"
                         Height="36"
                         VerticalContentAlignment="Center"/>
            </Grid>
        </DockPanel>
    </Border>
</Window>
