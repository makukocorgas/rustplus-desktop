<ui:FluentWindow x:Class="RustPlusDesk.Views.BackupPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="{DynamicResource BackupPasswordTitle}"
        Width="440" Height="280"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="True"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{DynamicResource BackupPasswordTitle}" ShowMaximize="False" ShowMinimize="False"/>

        <DockPanel Grid.Row="1" Margin="20,4,20,20">
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
                <TextBlock x:Name="TxtTitle"
                           Text="{DynamicResource BackupProtectionTitle}"
                           FontSize="18" FontWeight="SemiBold"
                           Foreground="{DynamicResource Accent}"
                           VerticalAlignment="Center"/>
                <Separator Background="{DynamicResource CardBorder}" Margin="0,8,0,0"/>
            </StackPanel>
 
            <!-- Bottom Buttons -->
            <Grid DockPanel.Dock="Bottom" Margin="0,15,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Button x:Name="BtnConfirm" Grid.Column="1" Content="{DynamicResource OK}" Appearance="Primary" Width="100" Height="32" Margin="0,0,8,0" Click="BtnConfirm_Click"/>
                <ui:Button x:Name="BtnCancel" Grid.Column="2" Content="{DynamicResource Cancel}" Appearance="Secondary" Width="100" Height="32" Click="BtnCancel_Click"/>
            </Grid>
 
            <!-- Main Content -->
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="TxtDescription" 
                           Text="{DynamicResource BackupProtectionDesc}" 
                           Foreground="{DynamicResource TextSubtle}" 
                           FontSize="12" 
                           TextWrapping="Wrap" 
                           Margin="0,0,0,12"/>
 
                <PasswordBox x:Name="PbPassword" 
                             Height="34"
                             Background="{DynamicResource Surface}" 
                             BorderBrush="{DynamicResource CardBorder}" 
                             BorderThickness="1" 
                             Foreground="White"
                             VerticalContentAlignment="Center"
                             Padding="8,0"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</ui:FluentWindow>
