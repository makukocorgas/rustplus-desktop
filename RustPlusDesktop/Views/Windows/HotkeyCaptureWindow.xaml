<ui:FluentWindow x:Class="RustPlusDesk.Views.Windows.HotkeyCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="{DynamicResource UiPressHotkey}"
        Height="230" Width="360"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Topmost="True"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{DynamicResource UiPressHotkey}" ShowMaximize="False" ShowMinimize="False"/>

        <Grid Grid.Row="1" Margin="20,4,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="{DynamicResource UiSetShortcut}"
                       FontSize="16" FontWeight="SemiBold" 
                       Foreground="{DynamicResource TextPrimary}"
                       HorizontalAlignment="Center"/>

            <!-- Visual Key Badge -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <ui:SymbolIcon Symbol="Keyboard24" FontSize="32" Foreground="{DynamicResource Accent}" Margin="0,0,0,10" />
                
                <Border Background="{DynamicResource Surface}" 
                        BorderBrush="{DynamicResource CardBorder}" 
                        BorderThickness="1.5" 
                        CornerRadius="6" 
                        Padding="16,8"
                        MinWidth="200">
                    <TextBlock x:Name="TxtGesture" 
                               Text="{DynamicResource UiPressDesiredKeyCombination}"
                               FontSize="14" 
                               FontWeight="SemiBold" 
                               Foreground="{DynamicResource TextPrimary}"
                               HorizontalAlignment="Center"
                               TextWrapping="NoWrap"/>
                </Border>
            </StackPanel>

            <!-- Footer Instructions -->
            <TextBlock Grid.Row="2" x:Name="TxtFooter" 
                       Text="{DynamicResource UiPressESCToCancelEnterToSave}"
                       FontSize="11" 
                       Foreground="{DynamicResource TextSubtle}"
                       HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
